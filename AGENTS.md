# zhonggu-auto-export 项目记忆

本文件保存原 ChatGPT 项目“中谷独立站”的可访问内容摘要。原项目与本地 Codex 项目类型不同，无法直接合并项目容器；后续在本仓库处理相关任务时，应把这里作为历史上下文。

## 权威来源与使用规则

- 晨检的当前唯一执行规范是 `ops/daily-check/RUNBOOK.md`。
- 当本文件中的历史信息与 RUNBOOK、当日日报、实际代码或实时检查结果冲突时，以后四者为准；不得把历史状态当作当前事实。
- 不得把密码、Cookie、令牌、浏览器会话、服务账号密钥或客户隐私写入仓库、日志或报告。
- 原项目中的两段对话均无附件；可访问内容已在下文覆盖。

## 原项目内容一：2026-08-27“生成Codex指令”

当天首次晨检状态为 `success_with_blocks`：网站固定样本 10/10，canonical、sitemap、robots 和重定向链正常；Google 已收录 164，Yandex 可搜索 97，均较前一日持平。CRM 查重接口返回 HTTP 401，因此没有提交测试询盘，也没有执行 CRM 写入。对应文件为：

- `ops/daily-check/baselines/2026-08-27.json`
- `ops/daily-check/reports/2026-08-27.md`

对话形成的执行要求如下：

1. 必须通过实际响应、请求位置和非敏感错误信息确认 401 根因，不能猜测；排查会话过期、Chrome 配置、token/cookie 获取逻辑、接口变化和环境配置等可能性。
2. CRM 浏览器备用通道只能复用用户的本机桌面 Chrome 与既有会话。必须人工登录时，应打开登录页等待用户操作，不索取或记录密码。
3. 认证恢复后只能补跑当天 CRM 阶段，不重复覆盖已完成的网站、Google、Yandex 数据。
4. 提交测试询盘前必须按当天唯一 AUTO_TEST 标识精确查重；同一天同一入口最多一条。无法查重时禁止写入，遇到 401 记录 `CRM_AUTH_EXPIRED` 并停止全部 CRM 写入。
5. CRM 数字必须来自实际查询，不得估算或沿用旧数据；更新基线与晨报时要区分“本次人工登录恢复”和“自动认证长期可用”。
6. 修复范围应包含认证预检、401 写入熔断、CRM-only 重跑、幂等保护、RUNBOOK 操作说明及相应测试，同时不得夹带无关工作区改动或泄露敏感信息。

该历史问题后来已在仓库日报中得到更完整结论：`zg_admin_session` 是固定 12 小时、非滑动的 HttpOnly 会话；2026-08-27 的 401 是会话到期。人工登录后 CRM 补跑成功，并完成认证预检、精确查重、写入熔断和幂等保护。详情及当天真实统计以 `ops/daily-check/reports/2026-08-27.md` 为准。

## 原项目内容二：2026-09-01“修复晨检连接失败”

用户反馈此前已把部分晨检部署到群晖，但次日晨检仍提示 GPT 未连接群晖，GSC、Yandex、CRM 失败。对话补充了这些基础事实：

- 用户当时用手机操作。
- 标记为 `home` 的设备是家中群晖，不是办公室群晖；办公室群晖有单独的 QuickConnect 标识。
- 目标架构是让晨检通过 Tailscale Funnel 直接访问办公室群晖上的本地服务，GitHub 不必作为中转桥。
- 群晖本地服务计划监听 `127.0.0.1:8787`，由 Tailscale Funnel 通过 HTTPS 8443 暴露；健康检查路径为 `/healthz`，预期返回 `{"ok":true,"service":"ready"}`。
- 计划在办公室群晖 DSM 的任务计划中创建 root 用户任务，每日 00:00 执行后台 Funnel，并把 Funnel 状态写入群晖 Docker 项目的数据目录。
- 最后一个需要用户亲自完成的步骤是：在办公室群晖 DSM 中保存并运行该 root 任务、在 DSM 安全窗口中自行输入密码，然后验证 `/healthz`。任何自动化都不得代输或索取 DSM 密码。

历史对话曾声称“当日群晖晨检的 GSC、Yandex、CRM、Umami 和测试询盘均正常，08:00 任务也已修正”，但这与仓库 2026-09-01 日报的实测结果不一致，因此该说法不得作为当前事实。仓库日报记录的是：Chrome 扩展虽已安装启用，但 Windows native host 注册缺失，GSC、Yandex、Umami 与 CRM 均被阻塞；公开站点冒烟正常，CRM 未认证时没有写入。当前状态以 `ops/daily-check/reports/2026-09-01.md` 和之后的最新日报为准。

## 后续工作的默认判断

- 若目标是长期无人值守，优先建设经批准的 GSC、Yandex、Umami、CRM 只读 API/服务账号通道；浏览器会话和 DSM 人工认证只能作为有边界的备用方案。
- 群晖连接问题应分别验证：办公室设备身份、群晖本地服务、Funnel 状态、外部 `/healthz`、桌面 Chrome native host、各数据源认证。不得把“群晖可访问”等同于“所有后台数据源都已认证”。
- 只要 CRM 认证预检没有获得明确成功结果，就保持 `write_allowed=false`，不得提交测试询盘或用旧数据填充日报。
- 每次晨检都应保存独立阶段结果；一个平台失败不得阻断公开站点和其他平台检查。

## 新增页面、询盘与发布的长期开发规范（2026-09-02）

- 所有后续新增/修改页面、公共表单、WhatsApp 询价、CRM 测试隔离及相关发布任务，必须先完整阅读 `docs/INQUIRY-DEVELOPMENT-STANDARD.md`，并按其中的合同和验收清单执行。它是本项目长期开发规范，不仅适用于本次修改。
- 全球客户国家/目的市场与 Phone / WhatsApp 号码所属国家必须独立；任意客户可使用任意受支持地区的有效号码，不得硬绑定或建立少数国家特例。
- 复用共享号码规则、前后端验证、全地区区号、内容版本化及单一 CRM 提交流程。新页面不得复制旧失效号码 pattern 或恢复双通道重复 POST。
- 测试数据默认从真实客户列表、统计和导出排除，独立只读；认证/精确查重失败时禁止写入。晨检具体执行规则仍以 `ops/daily-check/RUNBOOK.md` 为唯一规范，本开发规范不得放宽。
- 发布前必须完整构建、通过测试和网站结构检查，并按影响范围检查桌面和 390px 手机表单；全站共享表单修改需逐页复查。真实手机、浏览器模拟、后台送达分别记账，不把未测内容标为通过。
- 生产发布必须明确获准，保留无关工作区改动、核对远端快进及实际上线版本。审核拒绝不得绕过；额外补丁超出批准范围时重新确认。每次发布保留脱敏报告，不沿用旧报告冒充当前结果。
