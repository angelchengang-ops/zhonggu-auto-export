# 六类重点车型 SEO 实施任务（2026-09-17）

## 任务状态与授权边界

用户已批准按六类产品方案开发，并要求使用 Codex 实施。此文件是执行任务，不是完成报告。必须实际修改源代码、生成页面、运行测试并提交结果，不能仅复述方案。

- 仓库：`angelchengang-ops/zhonggu-auto-export`。
- 本次工作分支：`codex/seo-six-product-lines-20260917`。
- 起始 main 提交：`d2b0106cd66d5bb69f0554c63139ecf97fdcb047`。
- 仅在此工作分支提交，最终保留 PR 供验收。不得自行合并、推送 main、强推、删除其他分支或直接部署生产。生产发布需按既有规范另行获得明确许可。
- 不覆盖其他未提交修改，不操作其他项目或 NAS 服务，不改变社媒/开发信定时任务。
- 不新增付费服务，不索取/保存/打印凭据，不在仓库或报告中写客户隐私、账户会话或生产访问密钥。

## 必读与实现入口

先完整阅读根目录 `AGENTS.md`、`docs/INQUIRY-DEVELOPMENT-STANDARD.md`。涉及晨检时完整阅读 `ops/daily-check/RUNBOOK.md`，沿用其唯一执行、安全、认证、测试去重规范。

已确认当前构建命令包含：

- `scripts/generate-vehicle-pages.js`
- `scripts/generate-seo-upgrade-pages.js`
- `scripts/sync-central-asia-faq.js`
- `scripts/normalize-whatsapp-contact.js`
- `scripts/maintain-seo.js`
- `scripts/fix-published-seo.js`
- webmaster / analytics / favicon 注入
- `scripts/build-phone-input.js`
- `scripts/version-public-assets.js`

必须先定位车型数据源、专题定义及后处理逻辑，在源文件实现，避免新内容被后续构建覆盖。不要只改生成 HTML；若项目跟踪生成产物，按项目惯例同步提交。

## 总目标

重点产品：吉利缤越 / Geely Coolray（Binyue）、奔腾 B70、悦意03、奔腾小马、悦意07、二手纯电车。

目标是提升采购意图搜索词的可发现性和询价转化质量，不把全站平均排名当作每个车型都已进入前10，也不得承诺排名。保留现有全部车型、真实图片视频、询价功能、正式主域名及已经生效的 canonical/重定向策略。

## A. 盘点与基线

1. 为六类产品逐一列出现有主专题、详情页、列表入口、数据源、canonical、标题、H1、内链、询价入口、语言及当前元数据。
2. 再核实本任务提到的现有路径，以实际代码和可读取网页为准；`.html`、无后缀和目录别名须按现有策略处理，禁止制造重复索引页、重定向循环或把不同车辆都 canonical 到一个专题。
3. 记录执行前页面/链接/价格/年款及车型ID基线；价格、库存数量、年款、新旧状态等商业事实不得根据模型推测修改。
4. 当前如果无已授权 GSC/Yandex 数据通道，不寻找或泄露密钥，不填造搜索量/排名。只把实际取得的数据标为实测，其他为未取得。

## B. 页面与内容改动

### B1. 吉利缤越

核查并复用现有 Geely Coolray 出口页、`geely-coolray-ready-stock-nansha-port` 专题、Full Option 详情和阿尔及利亚专题。

- 车型出口页用于采购选型；南沙专题说明库存核实、实车媒体和报价/发运条件；配置详情用于具体SKU，不复制大段相同文本。
- 删除或改写面向搜索引擎的 `Priority Africa Keywords`、连续关键词罗列模块，替换为自然的买家信息和询价准备清单。
- 保留已有缤越真实照片和南沙视频，保留点击后加载视频和性能优化；不得编造新照片、现货数量、核实日期或运输时效。
- 核对同一SKU在数据源、卡片、正文和结构化数据的报价一致性。出现历史价格冲突先记录并保留经确认数据，不擅自调价。
- 阿尔及利亚专题维持独立采购用途；不批量生成只替换国家名称的门页。新法规或进口资格未经当前权威材料核实不增加。

### B2. 奔腾B70

优先深化现有 `used-bestune-b70-wholesale` 以及2021/2022/2023年款详情。

- 以现有真实年份、里程和报价数据构成可比较表，明确哪些是范围、哪些是已确认单车信息。
- 说明车况差异、检测资料可询问项、批量/混批采购与报价所需信息。不得承诺未核实的事故状况或营运性质。
- 年份详情与主采购专题互链；不把新车与二手车报价混合，不为近义词再建重复专题。

### B3. 悦意03

深化现有 `bestune-yueyi-03-wholesale`，连接实际445/565公里版本。

- 保留对应年款和测试标准。使用已有可信车型资料说明版本比较、电池/充电等已核实信息；缺失项引导买家索取，不能虚构参数、海外保修或实际续航。
- `JoyEE 03` / `Yueyi 03` 名称对应关系只有经品牌一手资料核实后自然补充。别名留在同一车型体系，不复制页面。

### B4. 奔腾小马

复用现有 Xiaoma 222公里详情（按实际路径确认）。补完整系列级采购内容与首页入口。

- 核实 `Pony` 与 `Xiaoma` 别名后自然使用，保留现有SKU/网址。
- 只有确有多个版本及独立比较用途时再建系列专题；若仅一个SKU，在现有页增强即可。
- 使用现有实车素材，说明已核实配置与询价清单。不得宣称任意市场可上牌、续航无损耗或国内供货价等于海外落地价。

### B5. 悦意07

核查现有 `used-bestune-yueyi-07-phev-export` 与SKU。此前计划提及的年款/新旧状态存在待核项，以当前经确认的车型数据为准，不自动改成更新年款。

- 明确插电混动 PHEV 属性，与二手纯电 BEV 分类分开。
- 保留已有实车照片视频；基于已有资料说明纯电续航标准、充电、里程/检测资料。
- 没有真实新车与二手车两种供应时，不为覆盖词而虚构一个供应类别。

### B6. 二手纯电车

核查是否已有独立分类，无则按现有生成器/视觉/表单规范补齐，建议路径需先检查冲突和canonical策略后确定。

- 展示真实可供二手 BEV；不能把B70燃油车、悦意07 PHEV、新车小马或新车悦意03标成二手纯电库存。
- 如果数据源中目前没有二手纯电库存，分类必须诚实显示“当前没有已核实的公开库存，可提交采购需求”，不生成假车辆、假价格或 InStock Product。
- 页面要有独立采购价值：采购需求字段、年款/里程/用途确认、电池检测资料所需项目、充电接口与目的地匹配的核查说明。不要为凑页面写空泛关键词文章。
- 电池健康度、检测日期/方法、事故/维修状态未知就留空并显示未取得/待核实；禁止填造SOH、零事故、出口合规或认证。
- 库存不足以形成有用索引内容时，记录限制并先完成诚实的采购需求入口，不用薄页凑索引数。

## C. 首页、内链、元数据与转化

1. 首页现有重点入口扩为六类，放到合理靠前位置，沿用网站风格与响应式布局。不得删除或限制原有全车型列表、图片、视频和公司介绍。
2. 复用现有相关品牌页/列表页，形成首页→主专题/主采购页→具体SKU的清晰内链，链接文案自然且描述性明确。
3. 相关页调整title、description、H1、OG、图片alt、面包屑等；描述须与正文和实际库存一致。不得隐藏关键词或只为关键词堆砌。
4. Product/Offer/Vehicle等结构化数据仅描述页面真实可见、已确认内容；不编造评价、评分、价格、库存或把采购请求页当现货商品。FAQ文本可以用于用户帮助，但不承诺FAQ富结果。
5. 保留共享询盘入口、来源车型/页面归因、全球号码规则及WhatsApp流程。优先不改共享提交逻辑；新页必须接入既有单一CRM提交和号码规则，不另写一个POST通道。
6. 不添加新的分析/广告第三方SDK，不把点击自动认定为有效询盘，不公开CRM统计或客户数据。
7. 若实施翻译必须是真正的页面内容翻译、独立可访问URL、正确互相指向的hreflang与自身canonical；只翻菜单不算完成，不生成未核实的地域合规承诺。本轮优先完成英语主页面，复用现有语言结构，新增翻译范围如实列出。

## D. 六类车型搜索监测（实现代码与测试，不冒充已有实测）

以下为18个候选采购词，不是已验证搜索量或目前排名：

- Coolray: `Geely Coolray price in China`; `Geely Coolray export from China`; `Geely Coolray full option price`。
- B70: `used Bestune B70 for sale`; `used Bestune B70 price`; `Bestune B70 wholesale China`。
- Yueyi03: `Bestune Yueyi 03 price`; `Bestune Joyee 03 price`; `Bestune Yueyi 03 export`（别名须核实）。
- Xiaoma: `Bestune Pony price in China`; `Bestune Xiaoma for sale`; `Bestune Pony export`（别名须核实）。
- Yueyi07: `Bestune Yueyi 07 price`; `used Bestune Yueyi 07`; `Bestune Yueyi 07 PHEV export`。
- UsedBEV: `used electric cars from China`; `used EV exporter China`; `used electric cars wholesale China`。

在现有晨检模块边界内增加版本化配置、纯函数报告/统计和单元测试，避免另建重复任务。字段至少包含产品线、候选词、实际查询词、目标/实际国家、页面、设备/搜索类型、数据有效日期、展现、点击、CTR、平均排名、是否低样本及数据状态。

- 默认比较两个不重叠、可取得的完整28天窗口，具体起止和时区要显示，遵守数据滞后。不要把缺失/匿名化查询行当0或第100名。
- 名次平均按展现加权，零展现/缺失排名为null；CTR使用clicks/impressions，不平均各行CTR。不混合Google与Yandex，不把全站平均前10当六类词前10。
- 国家维度不得用用户手机号码所属国推断。目标国家未从真实数据确认时配置为未选定，不编造已验证市场。
- 低样本门槛为可配置、明确标为内部监测规则，不冒称Google标准。样本不足前10单独标记，不能标为稳定达标。
- 保持原晨报schema与消费者兼容；新增字段采用非破坏方式，不改写历史基线。正式GSC API无法读取时输出“未取得”，报告测试只能用明确fixture。
- 只提交代码与说明，本任务不授权远程改NAS、部署晨检服务、增加计划任务或主动请求搜索引擎重新收录。

## E. 验收

按顺序实际执行并记录：

```text
npm run build
npm test
npm run check:site
```

- 先查清运行环境和项目要求，不为通过测试删断言、忽略失败或盲目升级Node/依赖。外部请求和生产存储必须模拟。
- 新增回归测试：六类入口与实际主承接页；BEV/PHEV分类；没有库存不显示InStock或假车辆；已确认报价/SKU未被改变；去除关键词罗列；相关canonical/hreflang/内链；新增报告缺失值/低样本/加权计算与旧schema兼容。
- 关键源内容重新构建后仍存在，不能被后处理覆盖；检查第二次构建是否稳定并解释差异。
- 在可用浏览器分别检查桌面和390px手机、图片/视频、菜单、表单/询价入口和横向溢出。没有浏览器就记录未测，不能把静态断言说成浏览器通过。
- 不提交真实客户表单、不打开真实WhatsApp会话、不发通知。生产端到端未测必须单独说明；既有隔离测试也要遵守RUNBOOK认证和当天去重，不绕过。
- 不提交sitemap到搜索引擎、不请求索引或点验证修复；只更新本仓库内正确的站点地图产物。

## F. 交付

将实际代码和必要生成产物提交到本分支，保留PR。另写独立实施报告（不要把本任务文件改成假完成）：

- 改动文件、六类最终承接路径、实际新增/增强内容；
- 起始/结果提交、执行环境、构建测试输出和失败项；
- 桌面/手机验证的真实覆盖与未测项；
- 未改变的价格/库存/客户数据和现有域名策略；
- 无法核实的名称/年款/参数、当前没有的真实库存；
- GSC/Yandex是否实际连接，数据覆盖与缺失；
- 明确状态是已提交待验收/待发布，而不是“已上线”或“已进入前10”。

如果云环境缺少必要能力，完成安全可做部分，逐项列出阻塞，不另开重复项目，不索取敏感凭据，不虚报完成。
